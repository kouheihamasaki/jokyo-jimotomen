<div class="container">
  <div class="row">

    <div class="col-9">
      <h2 class="midashi col-auto mt-2">コミュニティ一覧</h2>
      <h6 class="mt-3">（全<%= @community_all.count %>件中<%= @communities.count %>件を表示）</h6>
      <% @communities.each do |community| %>
        <div class="border-top mt-2">
          <h5 class="font-blue">
            <%= link_to community_path(community), class:'text-reset', style:"text-decoration: underline" do %>
             <%= community.title %>
            <% end %>
          </h5>
          <p>対象都道府県：<%= community.prefecture %></p>
          <p><%= community.introduction %></p>
        </div>
      <% end %>
      <div class="mt-3 mb-5"></div>
        <h5>こんなコミュニティがあったらな、、という要望はこちら</h5>
        <%= link_to "コミュニティリクエスト", new_community_reqs_path, class: 'btn btn-outline-secondary ml-3 mb-3' %>
    </div>


    <%= render 'public/homes/sidebar', user: @user, post_fav_best: @post_fav_best %>


  </div>
</div>
